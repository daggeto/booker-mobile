.item.item-avatar
  img[src="{{reservation.service_photo_url}}"]
  h2
    | {{reservation.event.start_at | amDateFormat: 'HH:mm' }} -
     {{reservation.event.end_at | amDateFormat: 'HH:mm' }}
  p {{reservation.event.service.name}}
